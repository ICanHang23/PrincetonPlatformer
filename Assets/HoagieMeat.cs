using UnityEngine;

public class HoagieMeat : MonoBehaviour
{
    GameObject buns;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //buns = transform.parent.gameObject;
        //Collider2D meatCollider = GetComponent<BoxCollider2D>();
        //Collider2D bunsCollider = buns.GetComponent<BoxCollider2D>();

        //GameObject player = GameObject.FindGameObjectWithTag("Player");
        //Collider2D playerCollider = player.GetComponent<BoxCollider2D>();

        //Physics2D.IgnoreCollision(meatCollider, bunsCollider);
        //Physics2D.IgnoreCollision(meatCollider, playerCollider);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
